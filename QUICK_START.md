# 🚀 Быстрый старт - Тестирование через Postman

## ✅ API Запущен и Готов к Тестированию!

API уже запущен на `http://localhost` (порт 80 - стандартный HTTP)

## 📦 Шаг 1: Импорт в Postman

1. Откройте Postman
2. Нажмите **Import** (кнопка вверху слева)
3. Перетащите файл: **IronNode.postman_collection.json**
4. Готово! Коллекция появится в списке

## 🔑 Шаг 2: Тестируйте API

### 1. Health Check (проверка что API работает)

```
GET http://localhost/health
```

Ожидаемый результат:
```json
{
  "status": "ok",
  "message": "API is running"
}
```

### 2. Login (получить токен)

В Postman найдите:
**Authentication** → **Login (Demo User)** → Нажмите **Send**

Запрос:
```json
POST http://localhost/api/v1/auth/login

{
  "email": "demo@example.com",
  "password": "password123"
}
```

**Важно:** После успешного Login токен автоматически сохранится в переменную `{{token}}` и будет использоваться во всех последующих запросах!

### 3. Get Profile

**User Profile** → **Get Profile** → Send

Запрос автоматически использует сохраненный токен:
```
GET http://localhost/api/v1/user/profile
Authorization: Bearer {{token}}
```

### 4. List Blockchain Nodes

**Blockchain Nodes** → **List All Nodes** → Send

Вы увидите список blockchain нод из базы данных:
- Ethereum Mainnet
- Polygon Mainnet
- BSC Mainnet



```json

{
  "jsonrpc": "2.0",
  "method": "eth_blockNumber",
  "params": [],
  "id": 1
}
```

**Примечание:** Сейчас возвращаются mock данные. Для реальных данных нужно подключить настоящие blockchain ноды.

## 📋 Все доступные endpoints

### Без авторизации:
- ✅ `GET /health` - проверка API
- ✅ `POST /api/v1/auth/register` - регистрация
- ✅ `POST /api/v1/auth/login` - вход

### С авторизацией (требуется токен):
- ✅ `GET /api/v1/user/profile` - профиль пользователя
- ✅ `GET /api/v1/blockchain/nodes` - список нод
- ✅ `GET /api/v1/blockchain/nodes/:id` - информация о ноде
- ✅ `GET /api/v1/analytics/usage` - статистика использования
- ✅ `GET /api/v1/analytics/requests` - история запросов
- ✅ `GET /api/v1/api-keys` - список API ключей
- ✅ `POST /api/v1/api-keys` - создать API ключ
- ✅ `DELETE /api/v1/api-keys/:id` - удалить API ключ

## 🎯 Сценарий тестирования

Рекомендуемый порядок:

1. **Health Check** - убедитесь что API работает
2. **Login (Demo User)** - получите токен (автоматически сохранится)
3. **Get Profile** - проверьте авторизацию
4. **List Blockchain Nodes** - получите список нод
6. **Get Usage Statistics** - посмотрите аналитику
7. **Create API Key** - создайте новый API ключ

## 💡 Полезные советы

### Автоматическое сохранение токена

Коллекция настроена так, что после Login токен автоматически сохраняется в переменную `{{token}}`. Вам не нужно копировать его вручную!

### Тестирование разных сценариев

1. **Создать нового пользователя:**
   - Authentication → Register New User
   - Затем Login с новыми credentials

2. **Тестировать разные blockchain:**

3. **Управление API ключами:**
   - API Keys → Create API Key
   - API Keys → List API Keys
   - API Keys → Delete API Key

## 🐛 Troubleshooting

### "Could not get response" или "Connection refused"

**Проблема:** API не запущен

**Решение:**
```bash
cd "C:/Users/backend/Desktop/Cloud AI/Go"
go run cmd/standalone-api/main.go
```

### "Unauthorized" ошибка

**Проблема:** Токен не сохранен или истек

**Решение:** Выполните Login снова

### Токен не сохраняется автоматически

**Решение:**
1. Откройте **Login (Demo User)** запрос
2. Перейдите на вкладку **Tests**
3. Убедитесь что там есть скрипт сохранения токена
4. После Send проверьте: Collections → IronNode API → Variables → token

## 📊 Что показывает API сейчас

### Реальные данные из базы:
- ✅ Пользователи (demo@example.com)
- ✅ Blockchain ноды (Ethereum, Polygon, BSC)
- ✅ JWT токены

### Mock данные (для демонстрации):
- 📝 Analytics/Usage stats
- 📝 Request history
- 📝 API keys (частично)

## 🔜 Следующие шаги

После тестирования базовых функций:

1. Установите protoc (см. SETUP.md)
2. Сгенерируйте protobuf файлы
3. Запустите все 7 микросервисов
4. Подключите реальные blockchain ноды
5. Настройте production окружение

## 📞 Нужна помощь?

Проверьте другие файлы документации:
- `SETUP.md` - полная инструкция по настройке
- `POSTMAN_TESTING.md` - подробное руководство по тестированию
- `README.md` - общая документация проекта

---

**Статус:** ✅ API работает и готов к тестированию!
**Порт:** 8080
**Demo user:** demo@example.com / password123
**Postman коллекция:** IronNode.postman_collection.json

Удачи! 🚀
