# Тестирование API через Postman

## 📦 Шаг 1: Импорт коллекции в Postman

1. Откройте Postman
2. Нажмите **Import** в левом верхнем углу
3. Выберите файл: `IronNode.postman_collection.json`
4. Коллекция появится в списке

## 🚀 Шаг 2: Запуск минимального набора сервисов

Для начала тестирования вам нужен только **API Gateway**. Остальные сервисы можно добавить позже.

### Вариант А: Только API Gateway (для быстрого старта)

Пока у нас нет protobuf файлов, API Gateway будет возвращать mock данные:

```bash
cd "C:/Users/backend/Desktop/Cloud AI/Go"
go run services/api-gateway/cmd/main.go
```

**Проблема:** API Gateway зависит от Auth Service (gRPC), который требует protobuf.

### Вариант Б: Создать упрощенную версию API Gateway

Давайте создадим standalone версию API Gateway без gRPC зависимостей для тестирования.

## 🔧 Шаг 3: Быстрое решение - Standalone API

Я создам для вас простой standalone HTTP API сервер для тестирования основных endpoint'ов без gRPC.

## ✅ Шаг 4: Тестирование в Postman

### 1. Health Check (без авторизации)

**Request:**
```
GET http://localhost:8080/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok"
}
```

### 2. Login (получить токен)

**Request:**
```
POST http://localhost:8080/api/v1/auth/login
Content-Type: application/json

{
  "email": "demo@example.com",
  "password": "password123"
}
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Login successful",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

**Важно:** Скопируйте токен! Он понадобится для следующих запросов.

### 3. Get Profile (с авторизацией)

**Request:**
```
GET http://localhost:8080/api/v1/user/profile
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Profile retrieved successfully",
  "data": {
    "user_id": "...",
    "email": "demo@example.com",
    "first_name": "Demo",
    "last_name": "User",
    "is_active": true
  }
}
```

### 4. List Blockchain Nodes

**Request:**
```
GET http://localhost:8080/api/v1/blockchain/nodes
Authorization: Bearer YOUR_TOKEN_HERE
```


**Request:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "method": "eth_blockNumber",
  "params": [],
  "id": 1
}
```

## 🔑 Использование переменных в Postman

Коллекция уже настроена с переменными:

1. **baseUrl** = `http://localhost:8080/api/v1`
2. **token** = автоматически сохраняется после Login

После успешного Login, токен автоматически сохранится и будет использоваться во всех запросах!

## 📝 Порядок тестирования

### Базовый сценарий:

1. ✅ **Health Check** - проверка что API работает
2. ✅ **Login (Demo User)** - получение токена
3. ✅ **Get Profile** - проверка авторизации
4. ✅ **List Blockchain Nodes** - получение списка нод
5. ✅ **Get Usage Statistics** - аналитика
6. ✅ **List API Keys** - список ключей

### Полный сценарий:

1. **Register New User** - создание нового пользователя
2. **Login** - вход с новым пользователем
3. **Create API Key** - создание API ключа
5. **Get Request History** - просмотр истории
6. **Delete API Key** - удаление ключа

## 🐛 Troubleshooting

### Ошибка: "Connection refused"
**Проблема:** API Gateway не запущен
**Решение:**
```bash
go run services/api-gateway/cmd/main.go
```

### Ошибка: "Unauthorized"
**Проблема:** Токен отсутствует или истек
**Решение:** Выполните Login снова

### Ошибка: "package proto is not in std"
**Проблема:** Не сгенерированы protobuf файлы
**Решение:** См. SETUP.md для генерации protobuf

## 🎯 Альтернатива: Тестирование через cURL

Если у вас нет Postman, можно использовать cURL:

```bash
# Login
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"demo@example.com","password":"password123"}'

# Получить профиль (замените TOKEN)
curl -X GET http://localhost:8080/api/v1/user/profile \
  -H "Authorization: Bearer YOUR_TOKEN"

# Список нод
curl -X GET http://localhost:8080/api/v1/blockchain/nodes \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📊 Ожидаемые статус коды

- **200** - OK (успешный запрос)
- **201** - Created (ресурс создан)
- **400** - Bad Request (неверные данные)
- **401** - Unauthorized (не авторизован)
- **403** - Forbidden (доступ запрещен)
- **404** - Not Found (не найдено)
- **429** - Too Many Requests (превышен rate limit)
- **500** - Internal Server Error (ошибка сервера)

## 🔄 Следующие шаги

После того как протестируете базовые endpoint'ы:

1. Установите protoc (см. SETUP.md)
2. Сгенерируйте protobuf файлы
3. Запустите все микросервисы
4. Протестируйте полную функциональность

Удачи! 🚀
